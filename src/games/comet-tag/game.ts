import type { GameMeta } from '../registry'

const game: GameMeta = {
  id: 'comet-tag',
  title: 'Comet Tag',
  mode: '1v1',
  status: 'Planned',
  description: 'Fast swaps, slow trails, tag before the comet fades.',
}

export default game
