import type { GameMeta } from '../registry'

const game: GameMeta = {
  id: 'halo-relay',
  title: 'Halo Relay',
  mode: 'Team Trial',
  status: 'Open',
  description: 'Pass the halo through checkpoints, one miss resets.',
}

export default game
