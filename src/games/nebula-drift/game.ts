import type { GameMeta } from '../registry'

const game: GameMeta = {
  id: 'nebula-drift',
  title: 'Nebula Drift',
  mode: 'Battle Run',
  status: 'Open',
  description: 'Draft behind rivals to boost into the last gate.',
}

export default game
